---
import { Icon } from 'astro-icon/components'
---

<div id="tooltip" class="hidden tooltip-box bg-white text-black h-20">
    <div class="flex justify-center items-center">
        <div class="flex flex-row justify-center content-between">
            <div class="flex">
                <Icon class="self-center w-32 flex-auto" name="screenshot" size={100}/>
            </div>
            <div class="pr-5"></div>
            <div class=" text-3xl flex-auto w-56 ">
                <span class="text-gray-400">พี่ๆ</span> <span>แคปหน้าจอนี้</span>
                <br>
                <span class="text-gray-400">แล้ว</span> <span>จ่ายในแอพธนาคาร</span>
                <br>
                <span class="text-gray-400">ได้เลยนะ</span>
            </div>
            <div class="flex flex-none w-12">
                <span id="closeTooltip" class="self-end top-28 right-10 text-3xl">X</span>
            </div>
        </div>

    </div>
</div>


<script>
    const closeTooltipButton = document.querySelector("#closeTooltip")
    closeTooltipButton?.addEventListener('click', () => {
        const tooltip = document.querySelector("#tooltip")
        tooltip?.classList.add("animate-slideUp")
        tooltip?.classList.remove("animate-slideDown")
        window.setTimeout(() => {
            tooltip?.classList.add("hidden")
        }, 1000)
    })
</script>

<style>

    .tooltip-box {
        position: fixed;
        width: 100%;
        height: 20%;
        border-bottom-right-radius: 50px;
        border-bottom-left-radius: 50px;
    }
</style>
